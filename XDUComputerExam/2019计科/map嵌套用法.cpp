#include <iostream>
#include <stack>
#include <map>

using namespace std;

int main() {
    // map<int, map<int, int>> doubleMap;
    // // 赋值方法
    // doubleMap[0][1] = 2;
    // for (map<int, map<int, int>>::iterator it = doubleMap.begin(); it != doubleMap.end(); it++) {
    //     for (map<int, int>::iterator it2 = it->second.begin(); it2 != it->second.end(); it2++) {
    //         cout << "row: " << it->first << ", begin: " << it2->first << ", end: " << it2->second << endl;
    //     }
    // }
    int ch;
    while (cin >> ch)

        return 0;
}
